spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://${MONGODB_USERNAME}:${MONGODB_PASSWORD}@${MONGODB_CLUSTER}/db1?retryWrites=true&w=majority}
      database: ${MONGODB_DATABASE:db1}

adafruit:
  io:
    username: ${ADAFRUIT_USERNAME}
    key: ${ADAFRUIT_KEY}
    mqtt:
      clientId: iot-backend-client